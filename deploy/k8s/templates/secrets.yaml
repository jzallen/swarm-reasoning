apiVersion: v1
kind: Secret
metadata:
  name: api-keys
  labels:
    {{- include "swarm-reasoning.labels" . | nindent 4 }}
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "REPLACE_ME"
  GOOGLE_FACTCHECK_API_KEY: "REPLACE_ME"
  NEWSAPI_KEY: "REPLACE_ME"
  POSTGRES_PASSWORD: "swarm"
